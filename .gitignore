# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.venv-host/
.pytest_cache/
.ruff_cache/
.ty_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Rdata

# Rendered R Markdown outputs (source-only convention)
analysis/*.html
analysis/*_files/
analysis/*_cache/

# Models
model/

# Patient video data (never commit)
videos

# Pose estimation output (never commit)
output
benchmark_output

# Camera calibration (patient-adjacent: identifying lab/rig geometry — never commit)
calibration/
calibration.json

# Secrets / credentials (safety net)
.env
.env.*
*.env

# OS
.DS_Store
Thumbs.db

# Posit
.positai/

# Logs (may capture session/patient paths)
*.log

# MediaPipe downloaded models
mediapipe/

# Serena LSP (Headroom): track project.yml only; cache/memories/local stay local
.serena/*
!.serena/project.yml
