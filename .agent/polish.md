# Polish register

Deferred off-spine improvements + data-tier remediation. Rows are born at deferral time in whatever session finds them (acceptance check written there, while the evidence is fresh). A review's out-of-contract register entries + rows left unadjudicated when MAIN closes at context reserve enter the same way, `why` prefixed `unruled:` — MAIN sized those rows without judging the finding. `/session-polish` is the sole consumer — stateless, any order, no milestone coupling.

Row schema — `pri` 1 (highest) … 3 · `size` S ≤15% window | M ≤35% | L = session · `where` = `file:line` or artifact · `why` = evidence pointer (SHA, run output, finding id) · `acceptance` = the check that must run green under MAIN's own rerun.

Lifecycle — done ⇒ prune the row in the same `<scope> (polish): …` commit · dead evidence pointer or acceptance check ⇒ append `stale(<why>)` in place, next `/session-roadmap` session re-rules it · `unruled:` row ⇒ test the finding against its evidence pointer before any edit, and a finding that fails is pruned with its disproof in the commit body rather than implemented · item implying spine work ⇒ move it to `spine?` below + report to the user.

Items run inside the artifact's existing assurance tier; tier raises, new units + scope-source changes belong to `/session-roadmap`.

## Items

| pri | size | where | item | why | acceptance |
| --- | ---- | ----- | ---- | --- | ---------- |
| 2 | S | `src/pose_estimation/rtmlib_openvino.py` | Detector-output sanity guard: raise, or warn once and fall back to CPU, when a detector returns scores outside `[0,1]`. Today only the `--det-device` default keeps the NPU-YOLOX corruption out of the data; `--det-device NPU`, or a device whose dynamic-shape support regresses, still yields silent garbage. Key the guard on the score range alone — GPU pads to the same 100 rows legitimately, so a row-count predicate false-positives there. | f3d18ad; synthetic zeros probe (`.agent/memory.md` → Device placement): NPU `dets` −0.1471…1.0215 vs GPU 0.0000 vs CPU dynamic `(1,1,5)` | Synthetic zeros probe on `yolox_m`: the guard fires on NPU and stays silent on CPU + GPU, no patient clip involved; `pytest` green |
| 1 | M | `analysis/{compare_clinical,longitudinal,clinical_correlation,clinical_dimreduce,temporal_clinical,explore_clinical}.R` | Route the six legacy clinical consumers through one central mode-aware reader, written by this item; retain tags, mode-specific output names, unit-bearing labels. | Six duplicated suffix/read/bind paths are safe only implicitly — one widened glob silently pools normalised-2D with metric-3D. M3.4 was going to supply the reader and was cut, so this item now owns it. | Synthetic mixed 2D+3D directory: each CLI emits separately named mode outputs with retained units, or rejects mixed input; 2D goldens unchanged; `pytest tests/test_r_pipeline.py` + full suite green in primary tree |
| 1 | S | `analysis/make_templates.R:32-49`, `analysis/validate_metadata.R:32-53` | Generalise metadata discovery via the central tagged reader, preserving exact 2D behaviour. | Both silently skip `_3d`; M3 ships its own aggregate path, so widening these generic CLIs is compatibility work. | Synthetic 2D-only, 3D-only, mixed directories yield explicit mode choice, no duplicate video keys, unchanged 2D templates/validation; R gate green |
| 2 | S | `analysis/features.R:410-426` | Exclude generated clinical/movement outputs from broad raw-landmark discovery. | Current `\\.csv$` glob can rescan generated clinical outputs; `_x_m` world3d columns yield "No coordinate columns" rather than a clean skip. | Directory holding raw 2D + world3d + generated clinical outputs processes only intended raw inputs; full suite green |
| 2 | M | `analysis/arthrose_diag.R:61-140` | Add a trusted world3d hand-screen path (3D joint angles, metric aperture label, producer-style gating) and evidence or downgrade the 40°/60°·s⁻¹ threshold wording. | Screen expects raw 2D-shaped hand columns and computes flexion in x/y, yet calls its threshold output a diagnosis; thresholds are code facts with no cited clinical source. | Synthetic 2D legacy unchanged; analytic 3D hand fixture uses x/y/z + metre labels + fail-closed diagnostics; docs claim matches evidence; full suite green |
| 2 | M | `analysis/clinical_features.R:492-508` | Optional arm-length / hand-span / object-width normalised metrics carrying denominator provenance, raw values retained. M3.4's registry was cut, so this item declares its own normaliser metadata. | Raw and normalised answer different estimands; the current shoulder-width ratio is not a standard anthropometric normaliser, and no denominator metadata exists. | Side-specific denominator fixtures emit separate `metric_id`/`unit`/`normalizer_id`; missing denominator yields no normalised estimate; raw outputs byte-identical; R gate green |
| 3 | S | six clinical consumer bootstraps + `analysis/utils.R:29-53` | Deduplicate the repeated 13-line script-dir/source bootstrap when those files are next touched. | Maintenance cost, not correctness. | One helper path works under `Rscript`, `source()`, and project-root REPL; consumer tests unchanged |
| 3 | S | `analysis/utils.R:59-87` | Make the generic summariser all-NA safe and warning-free. | `mean/min/max(..., na.rm = TRUE)` over an all-NA column emits `NaN`/`-Inf` plus warnings; warnings are test errors. | All-NA and mixed-finite fixtures return typed `NA_real_` without warnings; finite-case outputs unchanged; R gate green |
| 2 | M | `src/pose_estimation/run.py` det-device default + `docs/technical/environment.md` | Move the detector to the GPU. The CPU default exists because NPU corrupts YOLOX and GPU was unavailable; the GPU is now enabled and pads its dynamic output with `0.0000` scores instead of garbage, at ~22× CPU speed. Validate on frames that hold real detections before flipping the default, since the zeros probe cannot show whether true rows survive the padding. | Synthetic zeros probe: GPU `dets` 0.0000 / `labels` −1, median 9.7 ms vs CPU 213.1 ms; `EXECUTION_DEVICES` = `['GPU.0']`; `.agent/memory.md` → Device placement | On an input with real detections (public image or cleared clip), GPU and CPU agree on box count and on scores within tolerance, every score in `[0,1]`; batch wall-clock recorded for both; default flipped only if agreement holds; full suite green |
| 3 | S | `src/pose_estimation/rtmlib_openvino.py`, `src/pose_estimation/models.py` | Report the placement each compile actually got — read `compiled_model.get_property("EXECUTION_DEVICES")` and print that instead of the requested device name. Normalise the split return type first: NPU answers a bare `str`, GPU/CPU a `list` (`['GPU.0']`, `['CPU']`). | `CLAUDE.local.md` requires placement confirmation. Both sites print the *requested* device: truthful today only because exact device names are passed, and silently wrong the moment an `AUTO:`/`HETERO:` string reaches either call. | Compiling on each of NPU, GPU, CPU prints the device the property reports; an `AUTO:NPU,GPU,CPU` string prints the parenthesised selection rather than `AUTO:…`; full suite green |
| 2 | S | `scripts/regenerate_r_clinical_goldens.py:313-316`, `tests/test_r_clinical_goldens.py` | Add movement-phase artifacts to the golden matrix (2D and 3D). | No phase golden exists in either mode, so phase bytes have no oracle; M3.2 gave the 3D phase artifact nine tags and always-write semantics with only behavioural coverage. | `_DATASET_STEMS` expected-outputs contract emits phase files; byte/value/schema cases cover them; 2D phase goldens byte-identical across two regenerations; full suite green in primary tree |
| 3 | S | `.scratch/steq.py` → committed check | Port the ASD-STE100 register scan into committed state as a docs-tier check over the `docs/technical/conventions.md` → *Text register* inventory. Widen code-file sampling from `help=`/`description=`/`title=` to console `print()`/`cat`/`message` text, which the inventory covers and `.scratch/steq.py` skips. Keep `LONG` + `FILLER` as the failing signal; leave `PASSIVE` + `CONTRACTION` advisory, because both heuristics fire on legitimate prose (possessive `'s`, actor-free state descriptions). Payload strings named as code surface stay outside the scan. | Register conformance is claimed for `README.md`, `docs/capture_protocol.md`, `analysis/analysis_summary.Rmd` and the CLI + console strings, yet the only checker is gitignored — the claim cannot rerun from committed state. In-inventory baseline: `README.md` 1 finding / 1 failure. `docs/technical/` sits in the agent register by `conventions.md`, so its counts stay outside this row — a scan that mixes the two registers reports failures the project never claimed. | Clean checkout: the committed check reports 0 `LONG`/`FILLER` over the inventory files, and fails on a seeded 30-word instruction sentence; full suite green |
| 3 | S | `tests/test_r_qc_evidence.py` corpus, the 60 Hz `gap6`/`gap7` pair | Rebuild the pair on round-trip-precision timestamps, then assert the inferred cadence with `gap6` passing and `gap7` failing. | The pair reads as an exact 0.10 s boundary and is not one: `src/pose_estimation/export.py:505` writes 4-decimal timestamps, the producer re-infers `fs = 59.880239520958`, and the six-slot gap measures 0.1002 → already `gap_too_long`. The real boundary case is the 100 Hz pair, which `test_gap_threshold_tolerance_is_load_bearing` pins. Oracle-derived expectations keep the suite green either way, so this is clarity, not a defect. | The 60 Hz cases assert their inferred cadence, `gap6` passes and `gap7` fails `gap_too_long`; full suite green |
| 3 | S | `tests/test_r_qc_evidence.py` | Port `rev2-m3u3a-1`'s locale determinism sweep into the committed suite: run one populated fixture under `C`, `C.utf8` and `en_US.utf8`, require byte-identical artifacts. | The sweep passed as a review probe and left no gate. V30 determinism rests on radix collation, which `test_qc_sort_uses_radix_and_metric_rank` pins by instrumenting `order()`; a locale-conditional formatting or sort path that happens to match the ambient locale still has no committed check. | Three-locale subprocess run over one fixture yields one artifact digest; a seeded `method="auto"` sort turns it red; full suite green |
| 2 | S | `analysis/clinical_correlation.R:210-214`, `analysis/longitudinal.R:219-223` | Make explicit-file mode reject or correctly resolve a `_clinical_3d.csv` argument. | Both accept a 3D frame file directly, and their `_clinical\.csv$` → `_clinical_windows.csv` derivation cannot match `_3d`, so the frame file is read a second time as if it were the window file. Directory mode is safe; only the explicit-file path is exposed. | Passing `_clinical_3d.csv` to either CLI errors with a mode message, or resolves `_clinical_3d_windows.csv`; 2D explicit-file behaviour unchanged; R gate green |
| 2 | S | `src/pose_estimation/run.py:663-668` | Forward `--output-dir` in native rtmlib session mode. | `process_session()` is called without `output_dir=args.output_dir`, so `pose-estimation-run --session-dir … --output-dir X` silently writes to `<session_dir.parent>/output/<session_id>` and ignores `X`. MediaPipe session mode forwards it correctly (`src/pose_estimation/main.py:524-571`). M2 drives the corpus through this entry point. | `--output-dir` places rtmlib session artifacts under the requested root; MediaPipe session behaviour unchanged; full suite green |
| 3 | S | `src/pose_estimation/multicam.py:511-516`, `src/pose_estimation/run.py:651-661` | Stop announcing a `<cam>_diag.csv` that the rtmlib path never writes. | `process_session()` prints `Wrote diagnostics: {diag_path}` unconditionally, but `run.py`'s `_camera_processor` accepts `output_diag` and never uses it, so no such file appears. Console output names an absent artifact. | An rtmlib session run announces only files present on disk afterwards; a MediaPipe session run still announces its `_diag.csv`; full suite green |
| 3 | S | `src/pose_estimation/multicam.py:56-62,322-343` | Make symlink containment consistent across the three camera-resolution paths. | `_safe_resolve()` resolves through symlinks, so a manifest `file:` ref naming a link outside the session directory raises `SessionError`, while the `cam*` glob and the manifest-without-`file` lookup accept the identical link unchecked. Identical layouts, opposite verdicts. | A probe covering all three paths against one out-of-tree symlink target reaches the same verdict; **the M2.2 session generator depends on glob and manifest-without-`file` accepting such links, so any fix must keep those two accepting**; full suite green |
| 2 | S | `src/pose_estimation/inventory.py` `build_assets` | Source-stability detector: bind `size_bytes`, `content_sha256` and the header facts to one file version, so a replace/truncate/append between hash and probe cannot publish a plausible mixed row. | rev-m2u1 R23; the set-level coverage invariant catches a changed corpus, nothing catches a changed FILE | Replace and append to a synthetic video between hash and probe; expect a `read_error`-class row or status 2, never a canonical mixed row. |
| 3 | S | `src/pose_estimation/inventory.py` `run` | Refuse `--out` directories the pose-output scanners walk, or exclude inventory filenames in `analysis/` producers + `run_report.py`. | rev-m2u1 R24; both scan every top-level `*.csv`, so `assets.csv` enters those paths | Point `--out` at a directory holding a synthetic landmark CSV; expect status 2 before any write, or a passing scanner-exclusion test in both consumers. |
| 3 | S | `src/pose_estimation/inventory.py:1202-1218` | Keep console output outside the probe window, or print with `flush=True`. Every `print` in `main` currently runs after `run()` returns, so no line sits in a buffer while OpenCV is loaded; that safety is implicit and a progress line added inside the probe loop would reintroduce the hazard. A long checksum pass is exactly where someone adds one. | A native post-print fork inside the OpenCV stack flushes an already-buffered line a second time: four locale subprocess tests hit it, 1 duplicate by iteration 26, fixed by `PYTHONUNBUFFERED=1` in the child env (`tests/test_inventory_review.py`). Product side is clean today — `.scratch/probe_stdout_fork.py`, 30 redirected CLI runs, 7 identical lines each, 0 duplicates. | 30 redirected (non-TTY) CLI runs give byte-identical stdout with no repeated line, and a print placed inside the probe loop either flushes at once or fails a committed check |
| 3 | S | `src/pose_estimation/inventory.py` CLI | A CLI verb for `validate_generation`, so a human can check a published set without writing Python. | rev-m2u1 R22; the boundary ships library-only, and `--corpus` is required, so the verb needs its own argument group | `pose-estimation-inventory --verify <dir>` exits 0 on a valid set and 2 on each of the three tamper classes, with no path in the message. |
| 2 | M | `src/pose_estimation/sessions.py`, new `scripts/run_sessions_mutations.py` | Mutation campaign over the generator's predicates, mirroring `scripts/run_inventory_mutations.py`. Cover at least: the grain formatter, the conflict branch in `plan`, the `:02d` overflow guard, `_ESCAPE`'s lowercase-only hex, `_assert_owned`'s marker parse, `tree_digest`'s link/file split, the sibling staging paths, `_read_table`'s header check, every `fullmatch` alphabet (a mutant back to `match` must die on the trailing-newline cases), `_is_within`'s separator strip, `_assert_disjoint`'s two directions, the `OverflowError` arm of `_sweep_orphans`, and the sweep's position after the swap. | `.agent/archive/contract-m2u2.md` §10 deferred it at M2.2 dispatch. Memory's standing rule "a new predicate earns a mutant in the same commit" was written for the registry, and `sessions.py` shipped as a new module with its predicates pinned by tests alone. | Every mutant is killed by ≥1 committed test in `tests/test_sessions.py`; the script replays from a clean checkout to the same kill list; full suite green in the primary tree. |
| 1 | S | `tests/inventory_mutation_results.json:830-835`, `scripts/run_inventory_mutations.py:813-817` | Regenerate the committed mutation result against current `inventory.py`, or preserve the historical result under a versioned name and point the gate at a fresh output. | M2.1's mutation gate cannot replay from committed state — a project `CLAUDE.md` requirement. Recorded target `src/pose_estimation/inventory.py` = `96a6d0…` (at `9bccfe2`); committed HEAD = `66d3ef…`. `video_io.py` still matches at `fa3572b4…`. The runner refuses the default result file on any target mismatch, so the 71-killed/1-equivalent ruling currently certifies bytes that are no longer shipped. Verified by MAIN: `jq .target_sha256` vs `git show HEAD:<path> \| sha256sum`. | `scripts/run_inventory_mutations.py` runs from a clean checkout against the committed result file without a target-mismatch refusal, and reproduces the recorded kill list. |
| 2 | S | `src/pose_estimation/multicam.py:69-81,305-338`, `tests/test_multicam.py` | Reject booleans, numeric strings, non-integral floats and non-finite values for `sync_offset`; accept only a JSON integer ≥ 0, or supersede the field with M2.5's explicit offset/drift schema. | unruled: map-m2u3 R4. `int(entry.get("sync_offset", 0))` coerces before `SessionCamera` checks only negativity, so `true`, `"3"` and `2.7` all pass and `2.7` silently truncates. `tests/test_multicam.py` has sync tests but no strict-type case. | Each of `true`, `"3"`, `2.7`, `NaN`, `inf` is refused with a session error; integer ≥ 0 still accepted; existing sync behaviour unchanged; full suite green. |
| 2 | S | `src/pose_estimation/run.py:792-802`, `src/pose_estimation/main.py:531-535` | Catch `CalibrationError` alongside `SessionError` on both normal session entry points. | unruled: map-m2u3 R5. `discover_session` can raise `CalibrationError` (`src/pose_estimation/multicam.py:364-383`), but normal rtmlib dispatch and MediaPipe resolution catch only `SessionError`, so an invalid `calibration.json` exits by traceback. The redacted `--list-sessions` path already catches both (`src/pose_estimation/run.py:684-697`). | A session holding a malformed `calibration.json` reports the documented handled-error status with no traceback on both entry points; `--list-sessions` behaviour unchanged; full suite green. |
| 2 | S | `src/pose_estimation/calibration.py:98-186` | Reject non-finite numeric values and structurally unusable intrinsics (non-positive focal lengths, singular K) before a calibration reaches fusion. Keep physical-quality thresholds in qualification, not in the schema loader. | unruled: map-m2u3 R6. Validation checks list shapes, distortion length, resolution positivity, K's last row and world-frame zero pose, but never finiteness or invertibility; `src/pose_estimation/triangulation.py:26-74` passes the values straight into cv2 and linear algebra. A load-valid calibration can therefore be numerically unusable. | A calibration carrying `NaN`/`inf`, `fx <= 0`, or singular K is refused at load with a calibration error; every currently-valid fixture still loads unchanged; full suite green. |
| 2 | M | `src/pose_estimation/measure/rigidity.py` `_rigidity_metrics`, `MIN_VALID_FRACTION` | Raise rigidity measurement coverage **by re-anchoring, never by loosening `RANSAC_THRESHOLD_PX`**. Every sample is matched against `frames[0]` alone, so a clip whose scene changes past the baseline loses the whole asset rather than the affected span. Try a re-anchored or chained baseline that still reports drift against a single reference, and report per-span validity instead of one all-or-nothing fraction. Keep the reported quantity absolute drift from one reference — accumulated pairwise drift answers a different question than "does one calibration serve the whole clip". | At the shipped 8 px instrument, 81/379 assets carry no rigidity verdict: 71 support-unmeasurable + 10 orientation-excluded. The threshold route is already priced and refused: sweeping RANSAC to 32 px recovers 13 of the 71, and **all 13 land in `camera_motion`, none in `rigid`**, so it buys assets that fail the gate and no extrinsics (rulings R2, `scripts/probe_rigidity_saturation.py`). The failure concentrates in the single unstable cell rulings R1 names — iPad(5)/16.6 `above`, `valid_fraction` median 0.212 against 1.000 in every other cell, `decode_status` ok on 89/89 — which is what a re-anchored baseline addresses. Coverage bounds M2.6's yield, not its existence. | Corpus rerun at `RANSAC_THRESHOLD_PX = 8.0` unchanged reports strictly fewer than 71 `unmeasurable` assets **and strictly more than 280 `rigid`**, with no `rigid`→`camera_motion` flips among assets already measured, and the drift statistic stays a displacement from one reference; `pytest` green |
| 3 | M | `src/pose_estimation/inventory.py`, `sessions.py`, `qualify.py`, new `src/pose_estimation/publication.py` | Extract the shared publication contract — staging sibling, swap-then-sweep ordering, generation digest, `validate_generation`, `_is_within`/`_assert_disjoint`, `_assert_owned`, alphabet `fullmatch` helpers — into one module and route all three publishers through it. | Three modules each carry their own copy, and M2.3's sidecar decision (rulings A1) deliberately declined to add a fourth by making the sidecar a measurement record rather than a publication. The duplication is real and divergence between copies is the hazard; migrating three shipped publishers with mutation gates pinned to them is not M2.3's work. | All three publishers import the shared module; `scripts/run_inventory_mutations.py` reproduces its kill list; `scripts/check_inventory_determinism.py` green; published `inventory/`, `sessions/` and `qualification/` trees are byte-identical to their pre-refactor generations; full suite green in the primary tree |
| 2 | S | `scripts/probe_sync_policy.py` + `.agent/roadmap.md:60` | Reconcile the visual spike's family-connectivity figure. The port reproduces the visual estimator exactly on pairs (74/246) and closure (9 triangles), but the spike recorded **26/137** families where the probe computes **34/137** under both of its rules (one-camera-per-view, and every-asset). The audio case had the identical shape and resolved to a rule difference, not an estimator difference, so this is very likely a third connectivity definition rather than a defect. | Audio analog resolved at R6/R8: spike `_family_coverage` quantifies over one-asset-per-view selections with cross-view edges, giving 122 where whole-family connectivity gives 121. Visual spike rule lives in `.scratch/worktrees/spike-m2u3-visual` at `f82a9a9`. Probe run over `measurements/` prints `families_view_recoverable` 34 and `families_all_assets_connected` 34 for `visual_alone`. | Read the visual spike's connectivity function, name which rule yields 26, and either reproduce 26 under that rule in `probe_sync_policy.py` or amend roadmap:60 to the rule the probe uses — with the rule stated inline beside the number |
| 3 | S | `src/pose_estimation/measure/sync.py` + P36 | `drift_ppm`/`drift_se` carry no status column, so their emptiness (114/246 rows) never says which of `short_overlap`, `insufficient_windows`, `degenerate_regression` or `global_abstention` caused it. P36 froze the column list and nothing downstream consumes the reason (M2.5 takes no rate term, R5), so this is deferred rather than fixed. | R8 status matrix; measured 132/210 `ok` audio rows carry drift, 0/36 `low_confidence` rows do. `measure.DRIFT_STATUSES` already names the four tokens. | A consumer that needs the abstention reason exists, and `status_drift` is added to `SYNC_COLUMNS` with the corpus re-measured — or the column list is confirmed frozen with this row pruned as won't-fix |
| 2 | M | `src/pose_estimation/measure/` provenance blocks | Audit the shipped sync provenance against R8's V01-V13 list. P31 requires every constant, device, sampling rule and analysis resolution that moves the numbers; the list was accepted as the standard but the current block has never been checked against it item by item. Missing entries make the sidecar's numbers unattributable to the code that produced them. | R8 V01-V13, accepted as specification with ownership assigned to the `gate` track rather than as a passing claim. Current block printed by `measurements/measurements.json` `axes.sync.provenance`. | Each of V01-V13 maps to a named provenance key or to a written statement of why it cannot move the numbers, and D17's sweep shows every instrument parameter changing provenance and missing the cache |
| 3 | M | `src/pose_estimation/qualify.py` `_sync_strata`, `docs/technical/qualification.md` | Publish the `device_config` → `audio_rate_hz` multiplicity in `qualification.json`, so a configuration carrying more than one rate is stated by the artifact rather than re-derived from `assets_qc.csv`. P29's stratum exists to make a per-configuration constant visible; on this corpus the rate component adds no partition, and nothing published says when that stops being true. Emit the count of configurations whose rate set is not a singleton, and the offending keys. Schema change ⇒ `GENERATOR_VERSION` bump, corpus republish, determinism regeneration — which is why it is deferred rather than folded into P29. | `5e40922`; v3 republish over 379 assets measures 4 configurations each carrying exactly one rate — iPad(5)/16.7→44100 (131), iPad(5)/16.6→44100 (125), Air-M2/26.5→48000 (66), Air-M2/18.1.1→44100 (57) — so the redundancy is measured, not assumed, and is currently invisible to a consumer reading the census alone. | A synthetic fixture where one `device_config` carries two rates makes the census report that configuration by key with a non-zero count; the real corpus reports zero; `scripts/check_qualify_determinism.py` regenerated green after `rm -f`; full suite green in the primary tree |
| 3 | M | `src/pose_estimation/measure/__init__.py` `SUPPORTED_VERSIONS`, `GENERATION_KEYS` | Give the sidecar per-axis schema versions. The manifest already stores a version per axis entry, and A03 says an axis entry's own version names its generator — but `SUPPORTED_VERSIONS` is one module-wide frozenset, so a schema change on one axis invalidates every axis. Gate each axis on its own version and its own column set. | `9498278`: P29 widened `SYNC_COLUMNS` alone, and the resulting `v2`→`v3` bump orphaned the `detect` and `rigidity` tables whose schemas did not change, costing a full re-measure of both (detect wall 1387 s). Over-invalidation is a granularity defect, not a correctness one — the conservative behaviour is sound and is why it shipped. Filed by `rev4-m2u3-2` as register R02. | A single-axis schema change re-measures that axis alone: bump one axis's columns, confirm the other two axes' published tables stay accepted and byte-identical, and confirm a genuinely stale axis is still refused; `pytest` green |
| 3 | S | `scripts/`, `.agent/archive/contract-m2u3.md`, `.agent/roadmap.md` | Add a close-time consistency check: no predicate marked green in a frozen contract may still be described as open or remaining in `.agent/roadmap.md`. Run it as a script the close commit invokes, never as a unit test. | `9498278`: `rev4-m2u3-2` filed C18/C21 after reading a roadmap that still said `M2.3 OPEN` and `38 of 39` against committed state. The finding was correct; its proposed encoding — a `tests/test_qualify.py` case asserting roadmap prose — was rejected, because the roadmap is live state edited every window, the invariant is a process one owned by the mode's close order, and the test would put a planning document inside the qualifier's blast radius. Register R03. | Seeded contradiction (a contract predicate green while the roadmap calls it remaining) exits non-zero and names both locations; the tree at `9498278` exits zero; the check lives outside `tests/` and no suite depends on roadmap prose |

| 3 | S | `analysis/data_extraction.R:100-112`, `analysis/arthrose_diag.R:77-100` | Route both direct-delta angular-rate paths through the quantisation-robust cadence, or state in each why a per-row rounded interval is the right denominator there. | M2.4 contract §2 puts these out of scope so the unit stays one surface. Both divide angular change by each rounded row-to-row timestamp interval, which carries the same 4-decimal quantisation the unit fixes in the window/phase graph — at 30 fps an individual interval reads 0.0333 or 0.0334, a ±1.5 % per-row error that averages out over a window and does not over one row. Leaving them unrouted gives the project two cadence definitions in one language. | Synthetic 30 Hz and 29.97 Hz fixtures at 4-decimal precision: each script's angular-rate output either matches the robust-cadence denominator within 1e-4 relative, or carries an in-file statement of why the per-row interval is correct; existing 2D outputs unchanged; R gate green |

| 2 | S | `analysis/clinical_features.R:1294`, `:1300`, `:1305` | Publish a QC row for a person whose timestamps are unusable, instead of dropping the person silently. Three enumerator guards `next` before any window is keyed — non-finite or non-positive `nominal_fs`, non-finite `min`/`max`, span below one window — so a clip with an interior `Inf` or with all-identical timestamps emits **zero rows** and nothing records that it was seen. Emit one keyed row per affected person carrying the guard that fired, or state in each guard why silence is right there. | M2.4 ruling A33. `rev-m2u4-2` measured it: descending, duplicated and non-monotonic timestamps each publish 12 `invalid_timebase` rows, while interior `Inf`/`-Inf` and identical timestamps publish 0. `invalid_timebase` is a window-level ordering verdict reached only after a window is keyed (`:1332`), so a series pathological enough to destroy the span never reaches it. Pre-existing — the retired `median(diff(t))` estimator failed the same guard on the same inputs — which is why A33 keeps it out of M2.4 and why A04's three surviving classes stay a valid control. The roxygen at `:507` states the QC pass exists so one malformed clip records its defect rather than being lost, so the current behaviour contradicts its own stated purpose. | A fixture whose one person carries an interior `Inf` publishes that person's window rows with a QC reason naming the guard that fired, and a fixture whose timestamps are all identical does the same; the three ordering classes still publish 12 `invalid_timebase` rows each; every existing golden is byte-identical; R gate green |
| 3 | M | `analysis/clinical_features.R` `attach_artifact_tags()`, `docs/technical/analysis.md:64` | Give each 3D artifact its own version vector instead of one release triplet written across all of them. `attach_artifact_tags()` writes the same `producer_version`/`metric_method_version`/`qc_policy_version` to every 3D output of a run, so a bump in any one tag moves every 3D file — including files whose content is byte-identical to the previous release. A consumer cannot tell "this file changed" from "this release shipped" without hashing content. | M2.4 ruling A30, filed after the golden census measured exactly this: `world3d_clinical_3d.csv` moved in the three version tags **alone**, 91/91 rows, with no other cell touched. The docs now state the release-granularity reading explicitly so the current behaviour is at least not misleading. Deferred because per-file vectors change the published schema's meaning for every 3D consumer, which is a unit, not a fix. | A run that changes only the QC policy leaves `producer_version` and `metric_method_version` unmoved on the frame artifact whose content did not change; a run that changes nothing produces byte-identical 3D artifacts including tags; the release triplet stays recoverable from the set; R gate green |

| 3 | M | `analysis/clinical_features.R` `nominal_fs()` | Recover the true cadence when gap intervals outnumber base intervals, or return `NA_real_` instead of a confident blend. The `GAP_INTERVAL_FACTOR` filter assumes gaps are a minority; where they are not, `median(d)` is itself a gap, the filter retains every interval and the mean lands between the two populations. Interval statistics cannot separate the populations in that regime — a slot-count search over candidate base periods can, and would also subsume the existing filter. | M2.4 ruling A35. Measured: seven samples at 30 Hz spaced 2,2,2,2,1,1 slots return **18.0018 Hz**, and `trajectory_grid_status()` rejects it with `residual 0.401` against `GRID_SLOT_TOLERANCE = 0.25`, so the pipeline publishes `invalid_timebase`. Found by `rev2-m2u4-2` as a C6.08 baseline failure against unmutated MAIN. Deferred, not fixed, because the consumer already rejects the blend rather than trusting it — the cost is a lost window, not a wrong number. | A gap-majority fixture whose base cadence is 30 Hz either returns 30 Hz within 1e-4 or returns `NA_real_`; the 2,2,2,2,1,1 case above is covered; every currently-passing cadence case is unchanged and `tests/goldens/r_clinical/` is byte-identical; R gate green |

| 2 | S | `scripts/regenerate_r_clinical_goldens.py:322-342` | Inventory the staging directory's actual entries and kinds recursively, and fail on any entry outside `_expected_outputs(stem)` before the cleanup deletes it. The script copies the whitelist out of staging and then removes the directory, so an unexpected producer file, a temp file or a symlink is never seen by anything. | M2.4 ruling: filed by `rev-m2u4-3` as A21, medium. C2.14 inventories the destination, which is whitelist-derived by construction, so the case cannot observe the gap it appears to cover. Pre-existing and untouched by M2.4 — the unit widened an existing golden by two columns and added no file — so the gate stayed green and the shipped goldens are unaffected. The red test is preserved at `.agent/archive/m2u4-a21-credential.py.txt`: a monkeypatched producer emits `<stem>_unexpected.tmp` and regeneration succeeds instead of rejecting. | A producer emitting any entry outside the expected set fails regeneration by name and kind before cleanup, including a nested entry and a symlink; the unmodified producer regenerates all nine goldens byte-identically; R gate green |
| 3 | M | `tests/test_r_timebase_truth.py`, `analysis/clinical_features.R:455-469` | Add a deterministic gap-topology probe that derives `k` and `S_retained` independently of producer internals and asserts `abs(delta_fs / fs) <= k * TIMESTAMP_QUANTUM / S_retained`, reducing exactly to `TIMESTAMP_QUANTUM / span` at `k = 1`. | M2.4 ruling: filed by `rev-m2u4-3` as A34's assurance half, X01, P07, and A09's generalized half — one gap seen from four rows, medium. A09's contiguous half is closed in-unit by the one-ULP negative control; only the multi-run bound stays here. The documentation half is fixed at `df82a4b` and pinned by C2.15; this is the untested half. No behavioural defect exists — X01 records that the current estimator fits the ruled bound, so no red test can be written against today's code. Deferred because the bound has no runtime consumer: `trajectory_grid_status()` measures the slot residual directly rather than inferring it from the bound, and the census gates 21,651/21,651 windows under `nominal_fs` against 21,571/21,651 legacy. An unpinned inequality whose only consumer is a separately-measured residual is not a gate. Reversing this and pinning it inside M2.4 costs one probe file. | Independent topology oracle over deterministic 30/60/119.88 Hz drop patterns identifies retained runs and exposure without reading producer internals; every multi-run case satisfies `k * q / S_retained` and every one-run case reduces to `q / span`; off-by-one `k`, full-span-denominator and retained-count mutants each fail; the ruled 4.06x/5.08x/8.26x bound margins and the 1.686x whole-clip minimum reproduce; full suite green in primary tree |
| 2 | M | `analysis/clinical_features.R:1702`, `tests/test_r_timebase_truth.py` | Pin the movement-segmentation cadence numerically: predict an ascending phase value that separates `nominal_fs` from the legacy reciprocal-of-median cadence, assert the robust prediction, and keep the descending-group skip control. | M2.4 ruling: filed by `rev-m2u4-3` as X10, medium. C2.07 asserts the call site by regex over the R source, so it pins the call text and not the value's use — an overwrite or unused-call mutant survives it. C2.06's source-wide ban on the legacy formula kills the obvious reintroduction, and no phase golden exists, so the segmentation adoption carries no numeric oracle. No behavioural defect is claimed: `analysis/clinical_features.R:1702` does feed speed/NJ/SAL. | An ascending fixture's phase output matches the `nominal_fs` prediction and misses the legacy prediction by more than the assertion tolerance; a mutant overwriting `fs` after the call fails; descending groups keep default-mode skip; full suite green in primary tree |
| 3 | M | `tests/test_r_timebase_truth.py` | Add deterministic metamorphic property tests for `nominal_fs()`: origin translation, timestamp scaling, reversal under both magnitude modes, and randomized off-table rates and spans. | M2.4 ruling: filed by `rev-m2u4-3` as X09, medium. C7.08 covers origin and C8 covers three off-table corpus extrema, so the estimator is exercised but never characterized as a general function; a hard-coded-rate lookup, a scale-breaking edit, or a reversal-mode confusion stays mechanically possible. Source inspection confirms the estimator is general, so this is missing assurance rather than a defect. | Metamorphic relations hold across randomized off-table rates and spans; lookup, scale, and reversal-mode mutants each fail; seeds fixed so the suite stays deterministic; full suite green in primary tree |
| 3 | M | `scripts/probe_timebase_grid.py:504-505` | Bind published probe evidence to its inputs: record validated inventory, qualification, and selected-population digests plus an aggregate digest over selected asset ids and media hashes, and refuse to regenerate against stale inputs. | M2.4 ruling: filed by `rev-m2u4-3` as X14, medium. The results file binds only the generator digest, so altered corpus or inventory bytes still yield a source-bound-looking result. The 379-asset / 21,651-window aggregates are deterministic, redacted, and reproduced byte-identically, so the shipped numbers stand — the gap is provenance, on `data`-tier evidence a consumer re-validates at use. Publish digests only, never identities. `scout-m2u4` left an unmerged probe enhancement preserved at `.agent/archive/m2u4-scout-probe-terminal-duration.patch` (applies cleanly to main): it records whether the derived terminal frame duration falls inside the observed interval range, which is A31's header mechanism. Take it in the same pass, because adding a field forces the 379-asset census regeneration this item already requires. | Regeneration against altered inventory, qualification, or corpus bytes refuses instead of overwriting; the aggregate digest moves when the selected population moves; no filename, subject, or asset identifier appears in any published artifact |
| 3 | S | `docs/technical/analysis.md:22,297,383` + gate wiring | Split the three remaining pre-M2.4 descriptions over 25 words, then wire the sentence-length scan into the quality gate so the register becomes machine-decided. | M2.4 ruling: filed by `rev-m2u4-3` as X17, medium. M2.4's own two long sentences are fixed in-unit; these three predate the unit and fall outside its acceptance contract. `CLAUDE.md` Authoring caps descriptions at 25 words and no deterministic check owns that rule, so the register drifts silently between passes. | Scan reports zero sentences over 25 words in `docs/technical/analysis.md` outside tables and code blocks; the scan runs inside the gate and fails on a regression; terminology and published numbers unchanged |
| 3 | S | `analysis/clinical_features.R:502`, `tests/test_r_timebase_truth.py` | Cover `nominal_fs()`'s final guard: a non-finite or non-positive accepted interval mean must return `NA_real_`. | M2.4 ruling: filed by `rev2-m2u4-2` as M07, the one unencoded survivor of a 25-mutant campaign that killed 23. No committed test reaches the branch, so deleting or inverting it changes nothing observable. Reaching it needs an interval set whose gap-filtered mean is non-finite or non-positive, which the corpus does not contain — the same shape as the `magnitude` guard that had no case until `3061bd9`. Defensive branch, no behavioural defect. | A fixture drives the accepted mean non-finite or non-positive and the estimator returns `NA_real_`; deleting the guard fails that case; the 25-mutant campaign reruns with M07 killed |

## spine?

Findings a polish session judged spine work — ruled by `/session-roadmap`, not executed here. Row: `spine? <finding> | why: <evidence>`.

Empty. The `nominal_fs()` cadence finding was ruled spine and is now roadmap unit M2.4: M3's descope removed the "2D goldens byte-identical" bar that blocked adoption, and retiring `videos/initial/` removed the re-derivation question.
