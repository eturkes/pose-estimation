# M2.3 window log

Completed-window narrative for M2.3, moved out of `.agent/roadmap.md` (attached-state minimality).
Read on demand. Live M2.3 state, standing rulings and measured facts that bind M2.4-M2.7 stay in the
roadmap; this file is trajectory. Contract → `contract-m2u3.md`; verdict tables → `rulings-m2u3.md`.

Windows 1-4 were never written up per-window; window 1's sizing overrun is recorded in the roadmap.

**Window 6 shipped** `bd46b8f`..`d768ca7`, `main=` 76% 181K/240K. R9's P39 breach fixed: `conf_audio` was `min(peak_rms/threshold, peak_ratio/threshold)`, so accept thresholds rescaled a published statistic; it is replaced by raw `peak_rms_audio` with the status rebuilt as two raw comparisons, `GENERATOR_VERSION` bumped to `v2`, and the corpus re-measured. Status-preserving confirmed on real data, not asserted: 16/16 non-renamed columns byte-identical over all 246 pairs, `status_audio` ok 210 = 210, v1's confidence reconstructible on 246/246 rows, and the policy probe unchanged at 210/246, 122/137, closure 4.451/30.286 ms. R10 closed two of three gaps — `DOMAINS` bounds eight columns after the alphabet check, `MANIFEST_KEYS` closes the top level — with bounds chosen against measured corpus ranges and each check proven to fire. R3 closed negative on the scale survey. `scripts/check_qualify_determinism.py` imported from `wt/gate-m2u3`: 20/20 sweeps, 80/80 comparisons, 14/14 tamper classes.

**Window 7 shipped**, `main=` 69% 165K/240K, `mate=` 70% 168K/240K (`prod-m2u3-axes`). All four spine items landed — `measure.reconcile` (foreign key = hard error, omitted key = unmeasured, plus registry-cell agreement so a row cannot carry another family's `capture_id`), `ensure_cached` source-digest identity with the timing JSON landing last as trust anchor, `qualify.py --measurements` ingestion, and A09's declaration. The red suite harvested from `wt/test-m2u3-measure` `fd5df7a` opened at 115/33 against `2f5a9ef` and closed at **1044 passed / 1 failed**, the one red being `test_all_four_axis_modules_exist_and_name_their_axis`, which waits on `prod-m2u3-axes`'s two modules.

**The sync axis reached `events_qc.csv` in the same window, because ingesting it without that made the artifact contradict itself** — `qualification.json` claimed `measured_axes: [… sync …]` while all 193 event rows still read `sync_unmeasured`. P16 and P19 are now implemented (P19b, P19c added): membership comes from `placements.csv`, offsets solve breadth-first from the event's lowest asset id taking measured edges over accumulated paths, and closure publishes only where a three-camera event's three pairs are all accepted. Published: **173/193 events sync-qualified** (58/58 one-camera, 74/84 two-camera, 41/51 three-camera), 30 event triangles closing at median 5.403 / max 30.286 ms. **That is not the probe's closure population** — `probe_sync_policy.py` groups by capture family and accepts on audio alone, giving 35 triangles at 4.451/30.286 ms, and it still reruns exactly, so P38 holds. The shipped fused policy reads 117/137 families view-recoverable against audio-alone's 122/137. Neither number may be quoted for the other.

**P19c was a live defect, not a gap.** `_event_rows` re-derived `views` from the capture family, so the 7 single-camera events of the 2 view-conflict families published `above|left` and `above|left|right` — cameras those events do not hold. The session tree already publishes the correct per-event cell; the fix is to copy it.

**PAIRS_QC's `confidence` is now `peak_rms`**, and `status_audio`, `offset_visual_s`, `status_visual` joined it, so `status` is a pure function of published columns and R6 is re-rulable against the artifact rather than the sidecar. Corpus republished with `--measurements`: 246 pairs, **210/246 audio-accepted, 201 qualified** (56 corroborated + 145 uncorroborated, 9 contradicted, 9 visual-only, 27 neither). Determinism regenerated after `rm -f`: 20/20 sweeps, 14/14 tamper classes; `measure/scale.py` added to the script's `SOURCE_FILES` staleness tripwire.

**R2's rigidity denominator moved, and the ruling is amended.** Its headline table was measured before its own `RANSAC_THRESHOLD_PX = 8.0` decoupling took effect, so it reported the 4 px instrument's population under the 20 px gate — third-order consequence of P39. On the ruled implementation (`prod-m2u3-axes` `a626cb9`): **280/298 rigid (94.0%)**, camera_motion 18, support-unmeasurable 71, **81/379 carrying no verdict** against the recorded 93/379; all-members-rigid holds at **71/137** and R2's verdict is unchanged. Port drift is disproven, not assumed — the original script with its single constant set to 8 reproduces the port byte-identically on a source-order 20-asset oracle. **Closed in window 9: 298 is an instrument property and is published as one** — see *R2's saturation sweep* below.

**Window 8 shipped** — review intake, `main=` 90% 215K/240K, `mate=` 99% 238K/240K (`prod-m2u3-axes`). Suite **1057 passed / 1 failed**, the red still `test_all_four_axis_modules_exist_and_name_their_axis`. Eleven regressions harvested from the two reviewers' reds.

**All three teammates closed saturated (93-99%) and are stopped; their worktrees and branches are retained for successors to inherit.** `wt/prod-m2u3-axes` `b577774` carries `measure/rigidity.py`, `measure/detect.py`, their tests and both provenance blocks, unmerged and clean — 1377 lines MAIN did not review at 195K and must spot-check against the contract's invariant surfaces before merging. `wt/rev-m2u3` `43f7097` and `wt/rev2-m2u3` `51d56ec` carry the two review reports. A report emitted above ~90% is context-starved: re-derive rather than credit.

**R2's saturation sweep closed, and it answers against 298.** `scripts/probe_rigidity_saturation.py` holds `DRIFT_P95_GATE_PX = 20.0` at every row and sweeps `RANSAC_THRESHOLD_PX` over the 71 assets carrying no verdict at 8 px: **4/6/8 → 298, 12 → 304, 20 → 309, 32 → 311, 48 → 311**, growth per step `[0,0,6,5,2,0]`, so `saturated_above_baseline` is **false** — the plateau is 311 at 32 px. The sweep prices only no-verdict assets, and the monotonicity it relies on is spot-checked rather than assumed: **0/20** already-eligible assets lost at 48 px. **R2's verdict is unchanged and strengthened** — all 13 recovered assets are `camera_motion` and none is `rigid`, so rigid **280** and families **71/137** are invariant across the whole 4–48 px sweep. A loosened instrument buys assets that fail the gate, never extrinsics. Every rigidity population figure now reads "at RANSAC 8 px"; a denominator quoted without its threshold is misquoted. This complements `prod-m2u3-axes`'s U2, which showed **13/16** measurable `drift_p95_px` trajectories nonmonotonic under the same gate — the decoupling evidence, and the exact inverse of P21's pathology where the statistic tracked its own cut.

*The trust root was the hole.* `qualification.json` is excluded from `tree_digest` by construction — it carries that digest — and `census_digest` excluded the whole `generation` block on top of that, so the provenance block was the one published claim nothing covered, and the marker's own file identity was unchecked. One class, five filed consequences: a forged or deleted `measurements` key (rev F3), a duplicate JSON key resolving last-wins (F6a), a symlinked marker certifying a set from outside it (F6b), and — worst — `_assert_owned` following that symlink or accepting any foreign tool's marker, so a directory this tool never published licensed its own recursive deletion (F7). Fixes: `census_digest` now covers `generation` minus its own self-referential `census` key; one `_read_marker` (regular non-symlink via `lstat`, duplicate-key-rejecting parse) serves `_assert_owned`, `validate_generation` and `run`'s readback; `_is_own_generation` requires this generator's version and a closed key set, deliberately **without** digest freshness so a stale owned tree stays replaceable.

**The bound being claimed is detection, not authentication.** A set carries no key. Anyone who rewrites a claim and recomputes the census digest produces a document this check accepts, and no self-contained artifact can do better. What is ruled out is corruption and every edit that stops at the claim. Stated in `qualify.py` and in the consumer docs; do not let a later pass restate this as tamper-proofing.

Sidecar type discipline (rev F4, F5, F8, F10): `generator_version` is `isinstance`-guarded before the frozenset membership test that would otherwise raise a raw `TypeError`; finiteness moved into `_assert_cells` at the decimal alphabet, since `DOMAINS` leaves `offset_audio_s` and `drift_ppm` unbounded and a 400-digit cell reaches `inf` through both routes; `provenance` must be a JSON object with its nested keys still open; `out.iterdir()` wrapped as `sidecar_unreadable`.

**Determinism oracle rebuilt (rev2 #1, #2), schema 3: 40/40 sweeps, 19/19 tamper classes.** Measured mode was entirely outside it — every sweep ran flagless, so the whole window-7 ingestion path was unoracled. Both modes now run the full variation set against their own baseline, and the fixture builds its sidecar from `enumerate_pairs` itself so it cannot drift unreconcilable. `tested_head` is **removed, not regenerated**: the run that writes this file always precedes the commit that carries it, so any SHA recorded is the parent's and can never be checked — `source_sha256` is the real dependency and already binds it. Two tamper classes now gate F3 directly (`forged`/`removed measurements provenance`).

**R9's P39 fix left a false comment behind (rev2 #4).** `audio_offset.py` claimed accept thresholds "never enter" the published statistics and leave "every measured value byte-identical" — true of the pair gate `MIN_PEAK_RMS`/`MIN_PEAK_RATIO`, false of `LOCAL_MIN_PEAK_RMS`/`LOCAL_MIN_PEAK_RATIO`, which select the drift regression's windows and so shape `drift_ppm`, `drift_se` and `window_count`. **Ruled not a P39 breach**: the two roles already live in separate constants, both recorded in provenance, and no published statistic is compared against the LOCAL pair. The comment was the defect. Two MAIN probes failed to find a status-`ok` regime where a plausible gate change moves the value — the reproduced effect is the extreme one, a gate high enough to empty the window set.

**Window 9 shipped** — the axis harvest, `main=` 62% 149K/240K, `mate=` 63% 152K/240K (`rev3-m2u3`). Suite **1080 passed / 0 failed**; the long-standing `test_all_four_axis_modules_exist_and_name_their_axis` red is cleared. Measured axes are now **detect, orientation, rigidity, sync, timebase**, leaving `scale` alone unmeasured — the sentinel sweep finds no `rigidity_unmeasured` or `detect_unmeasured` in any table. `events_qc.csv`'s `geom_unmeasured` on all 193 rows is consistent and stays: `geom` is an event-level extrinsics reason token owned by M2.6, not one of the five evidence axes.

MAIN re-measured both axes rather than crediting the teammate's report. **Rigidity**, 379 rows: rigid 280, camera_motion 18, eligible 298, unmeasurable 71, excluded_orientation 10, error 0, no_verdict 81, multi_asset_families 137, all_members_rigid 71, wall 1117 s — identical to the teammate's output, so the harvest is credited by reproduction. **Detect**, 379 rows: sampled 9096, inferred 9096, 0 inference failures, 0 sample shortfalls, 0 error assets, `detect_rate` median 1.0 / mean 0.989886 / min 0.333333, wall 1654 s, detector on `GPU` and pose on `NPU`.

**The MP4 timed-metadata parser was duplicated across both harvested modules** — `_atoms`, `_declared_keys`, `_sample_entries` byte-identical, `_metadata_key_maps` divergent. Deduplicated into `measure/mebx.py` keeping detect's stricter entry-bounded implementation, and the equivalence was proven on real data before the swap: **0/379 key-map disagreements**, orientation status 369 constant / 7 changed / 3 missing both ways, reproducing F4 exactly. `scripts/probe_ransac_support.py` was **dropped, not merged** — it hardcodes `eligible_full_population = 286 + n`, the retired 4 px denominator, and answers a 4-vs-8 transition rather than saturation. Its family-invariant test survives, retargeted at the shipped `rigidity.summarize`, which is what publishes 71/137.

**Two producers retired**: `scripts/geometry_qualification.py` and `scripts/m2u3_detectability.py`. Both were superseded by the harvested modules, and the first still carried the P39 class itself — one constant serving as both accept gate and MAGSAC threshold. A retired script nothing calls is still quotable by a future agent.

**rev2 U11 + U10 closed** (`tests/test_measure_cache.py`, 4 red pre-fix / 6 pass post-fix, the sixth a positive control): `visual_offset._cache_valid` ignored `rotation_deg` though the writer already stored it; `audio_offset` decode facts were covered by no digest while `source_rate` is published as `same_audio_rate`, fixed with `cache.facts_sha256` over the facts minus its own block; `TARGET_RATE`/`COARSE_RATE` joined cache identity, because every recorded digest stays correct when a rate moves — the arrays are the ones written, at the old rate. **The determinism tripwire's `SOURCE_FILES` listed `scale.py` alone of the axis modules**; `sync.py`, `audio_offset.py`, `visual_offset.py` were never listed. All nine `measure/*.py` are now listed and pinned by `test_every_measure_module_reaches_the_determinism_tripwire`. Determinism regenerated after `rm -f`: **40/40 sweeps, 19/19 tamper classes**.

**Mutation campaign re-derived by MAIN after correcting the runner's `TEST_COMMAND`**, which carried a stale `-k` exclusion for a red this harvest turned green and omitted the new cache pins: **18 mutants / 17 killed / M02 equivalent / 0 survivors**, committed at `tests/measure_mutation_results.json`. The recorded command names `python`, never `sys.executable`, so the result binds to the command and not to the machine that ran it.

**P27 and P28 closed as claim-boundary work.** `docs/technical/qualification.md` gains a *Timing limits* section, and the roadmap carries the same two facts: rolling shutter is a **0–33.33 ms sweep** with a **12.4–30.9 ms** proxy from other Apple mobile devices, never a measurement of these iPads and never negligible; AAC priming reaches the estimator as a **measured 0 ms residual**, with the 3.891 ms mixed-rate prediction cancelled by PyAV's trim (skip 2112 samples on 379/379, first decoded PTS 0 on 379/379). **P19 closed**: `load_placements` appended every placed row, so a duplicate placement inflated an event's camera count and could read a connected event as disconnected — it now refuses same-event and cross-event repeats.

**Next window's spine order.** (1) **P29 is the one unmet contract predicate left** — sync QC must stratify by `(model, OS, sample_rate)`, and `qualify.py` publishes `device_config` plus a boolean `same_audio_rate`, so the exact rate stratum is unpublishable. The rate is already computed in `measure/sync.py:106-107` from `audio_offset.source_rate` and then discarded into a boolean; publishing it is a sidecar + `pairs_qc.csv` schema change, so it bumps `GENERATOR_VERSION` v2→v3, re-digests the column set, and needs a corpus republish plus a determinism regeneration. Acceptance: `tests/test_qualify.py::test_sync_qc_is_stratified_by_model_os_and_sample_rate`, where fixtures differing in one tuple component alone land in distinct strata. (2) Retire `U22`'s register residual — two 23-word imperatives in `qualification.md` and the completed-window narrative here, which belongs in `.agent/archive/`. (3) With P29 green and every review row adjudicated, M2.3 closes and M2.5/M2.6/M2.7 unblock on its ruling. The red suite stays diff-blind: MAIN must never make `tests/test_measure_diff_blind.py` read MAIN's own `tests/test_measure.py`.

**`rev-m2u3`'s F2 is ruled and closed; do not reopen it.** A21/P34's "flagless output stays byte-identical" governs the `generation` key set, not CSV bytes, and holds *within* a generator version. The decisive argument is that the wide reading forbids R9's own ruled `confidence`→`peak_rms` rename, and two accepted rulings cannot both bind. The accepted substitute is `GENERATOR_VERSION` v2 plus the column-set digest pin. The reviewer re-filed a strengthened red against this ruling twice while at 97% context — treat a saturated reviewer's persistence as a context artifact, not as evidence.

**Window 5 state.** Sidecar shipped (`src/pose_estimation/measure/`, `4baa307`) and its ingestion path
hardened: `load_axis` validated only the header, so cells, keys, duplicates, row count and row order
went unchecked on the one path whose whole premise is re-validating an independently produced record.
`validate` now returns the bytes it digested, closing the reopen window. R6 re-ruled P17b on
measurement; R8 batch-ruled all 138 rows of `test-m2u3-measure`'s PREP-1. **P38 is verified in full**
— the port reproduces 210/246 acceptance, 122/137 view-recoverable families and 4.451/30.286 ms
closure. (Spine work named here — R9's P39 fix, ingestion, R3 — all closed in windows 6 and 7.)

**P39 was red against window-5 code, and R9 fixed it in window 6.** `audio_offset.py:286` divided the accept thresholds into
the published confidence — `min(peak_rms / MIN_PEAK_RMS, peak_ratio / MIN_PEAK_RATIO)`, gated at
`< 1.0` — so sweeping a gate alone moves `conf_audio` by nine orders of magnitude while lag,
peak_rms, peak_ratio and overlap hold. Found by `test-m2u3-measure`'s independent D16 probe, verified
in source. This is R2's defect in a second place. Fix: publish raw statistics
(`conf_audio` → `peak_rms_audio`), gate by comparison, same on the drift path's `LOCAL_*` constants.
Status-preserving by construction, so the re-measured corpus must still return 210/246 and 122/137.
