# Session polish

Use spare capacity to execute deferred-perfection items from `.agent/polish.md`. Stateless + any-order; milestone/unit state remains `/session-roadmap`-owned; stop/repeat at any point.

- State arrives attached: @.agent/polish.md @.agent/memory.md. Start when both paths expand and `.agent/polish.md` contains items; an unexpanded path → `/session-roadmap`'s `ls .agent/` check; an empty register ⇒ report state + stop.
- Pick by `pri` where an entry carries one, else by blocking risk; then size-fit to the remaining window (S ≈ ≤15%, M ≈ ≤35%, L = session). Valid evidence pointer + acceptance check are prerequisites. On prerequisite drift, mark `stale(<why>)` in place + continue; the next `/session-roadmap` session re-rules it.
- Execute one item at a time under project gates; teammate use, worktrees, hygiene + Close order = session-roadmap rules. Completion = acceptance check green under MAIN's rerun → one scoped commit `<scope> (polish): …`, pruning its row in the same commit.
- Boundary: use the artifact's existing assurance tier, unit set + scope sources. The register = this session's sole scope-source edit. Route tier raises, new units, unadjudicated review rows + other scope-source changes through `/session-roadmap` — a unit's own check set belongs to MILESTONE-REVIEW, and this register carries out-of-contract items alone. A spine-work finding ⇒ append `spine? <finding> | why: <evidence>` + report it to the user.
- Close: register current (done rows pruned, stale rows marked), scoped commits, report = done | skipped(stale) | flagged-spine.

Task: $ARGUMENTS
